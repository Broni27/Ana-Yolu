# Ana Yolu Clinic - Инструкции по запуску

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск сервера разработки
```bash
npm run dev
```

Сайт будет доступен по адресу: `http://localhost:5173`

### 3. Сборка для продакшена
```bash
npm run build
```

### 4. Предпросмотр продакшен сборки
```bash
npm run preview
```

## 🎨 Особенности проекта

### Многоязычность
- **Азербайджанский** (az) - язык по умолчанию
- **Английский** (en)
- Переключение языка через кнопку в header (иконка глобуса)

### Темы
- **Светлая тема** - для дневного просмотра
- **Темная тема** - для ночного просмотра
- По умолчанию определяется системная тема пользователя
- Переключение через кнопку в header (иконка солнца/луны)

### Страницы
1. **Главная (/)** - Hero секция с 3D анимацией
2. **Команда (/team)** - Наши специалисты
3. **Услуги (/services)** - Все IVF услуги
4. **О нас (/about)** - История и миссия клиники

### 3D Анимации
- **Background3D** - Анимированная 3D сфера на главной странице
- **ParticlesBackground** - Частицы на других страницах
- Используется Three.js и React Three Fiber

### Адаптивность
- ✅ Desktop (1920px и выше)
- ✅ Laptop (1024px - 1920px)
- ✅ Tablet (768px - 1024px)
- ✅ Mobile (320px - 768px)

## 📁 Структура проекта

```
Animated/
├── public/
│   └── logo.png              # Логотип клиники
├── src/
│   ├── components/           # React компоненты
│   │   ├── Header.tsx
│   │   ├── Footer.tsx
│   │   ├── Background3D.tsx
│   │   ├── ParticlesBackground.tsx
│   │   ├── LoadingSpinner.tsx
│   │   ├── ScrollToTop.tsx
│   │   └── PageTransition.tsx
│   ├── pages/               # Страницы
│   │   ├── Home.tsx
│   │   ├── Team.tsx
│   │   ├── Services.tsx
│   │   └── About.tsx
│   ├── styles/              # CSS стили
│   │   └── global.css
│   ├── contexts/            # React контексты
│   │   └── ThemeContext.tsx
│   ├── hooks/               # Кастомные хуки
│   │   └── useScrollAnimation.ts
│   ├── i18n/                # Переводы
│   │   ├── index.ts
│   │   └── locales/
│   │       ├── az.json
│   │       └── en.json
│   ├── App.tsx
│   └── main.tsx
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
└── README.md
```

## 🎯 Технологии

- **React 18** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик
- **Tailwind CSS** - Стили
- **Three.js** - 3D графика
- **React Three Fiber** - Three.js для React
- **Framer Motion** - Анимации
- **React Router** - Маршрутизация
- **i18next** - Интернационализация
- **Lucide React** - Иконки

## 🎨 Цветовая палитра

- **Основной:** Белый (#FFFFFF)
- **Золотой:** #eab308 (и оттенки)
- **Акцент:** Черный (#000000)

## 📝 Добавление контента

### Как добавить перевод

1. Откройте `src/i18n/locales/az.json` для азербайджанского
2. Откройте `src/i18n/locales/en.json` для английского
3. Добавьте новые ключи и значения
4. Используйте `t('your.key')` в компонентах

### Как добавить нового сотрудника

Откройте `src/pages/Team.tsx` и добавьте объект в массив `teamMembers`:

```typescript
{
  name: 'Dr. John Doe',
  role: 'Specialist',
  experience: '10+ years',
  specialization: 'IVF',
}
```

### Как добавить новую услугу

Откройте `src/pages/Services.tsx` и добавьте объект в массив `services`:

```typescript
{
  icon: <TestTube className="w-8 h-8" />,
  title: 'New Service',
  description: 'Service description',
  features: ['Feature 1', 'Feature 2'],
}
```

## 🔧 Настройка

### Изменить порт разработки

В `vite.config.ts`:

```typescript
export default defineConfig({
  server: {
    port: 3000
  },
  // ...
})
```

### Изменить цвета

В `tailwind.config.js` в секции `theme.extend.colors`

## 📞 Поддержка

Если возникли вопросы или проблемы, обратитесь к разработчику.

## 📄 Лицензия

© 2025 Ana Yolu Clinic. Все права защищены.

